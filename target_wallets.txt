# ═══════════════════════════════════════════════════════════════════════════
# Verified target wallets — populated from the GMGN_TARGET_WALLETS secret in CI,
# or from the (gitignored) target_wallets.local.txt for local runs.
#
# The wallet overlap gate fires only when >= CONFLUENCE_MIN_WALLET_MATCH of
# these addresses appear among a token's top holders. This list is the bot's
# core signal, so the REAL addresses are never committed to git:
#   - locally:  target_wallets.local.txt (gitignored)
#   - in CI:    the GMGN_TARGET_WALLETS GitHub Action secret (space/newline sep)
#
# Keep only template/placeholder content here so the repo is safe to be public.
# ═══════════════════════════════════════════════════════════════════════════

# Example (delete these):
# ExampleWallet11111111111111111111111111111111
# 0x000000000000000000000000000000000000dEaD
